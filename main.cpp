#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "header.hpp"

// constexpr std::vector<int>DrawnNumbers{2,5,10,20,27};
// constexpr std::vector<int>vector1{1,2,3,6,12};

int main()
{
print(mapka);
CheckNums(DrawnNumbers, vector1);
CheckNums(DrawnNumbers, vector2);
CheckNums(DrawnNumbers, vector3);
CheckNums(DrawnNumbers, vector4);
CheckNums(DrawnNumbers, vector5);
CheckNums(DrawnNumbers, vector6);
print(mapka);
return 0;

}
